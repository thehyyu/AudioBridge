# 環境設定：BlackHole + 多重輸出 / 聚集裝置

這份教學只需要做一次。做完之後，`scripts/record.sh` 就能持續使用同一套裝置錄音。

## 1. 安裝 BlackHole

```
brew install blackhole-2ch
```

裝完後**登出再登入**（或重開機），macOS 才會正確載入這個虛擬音效卡。

## 2. 安裝 ffmpeg

```
brew install ffmpeg
```

## 3. 建立「多重輸出裝置」（讓你自己還聽得到聲音）

1. 打開 **音訊 MIDI 設定**（Spotlight 搜尋 "Audio MIDI Setup"）
2. 左下角點 `+` → **建立多重輸出裝置**
3. 勾選：你平常的輸出（例如耳機 / 喇叭）＋ **BlackHole 2ch**
4. 建議把你平常的輸出設為「主要裝置」，並確保它的時脈與 BlackHole 對齊（用同一個 sample rate，例如 44100 Hz）
5. 到 **系統設定 → 聲音 → 輸出**，選這個新建立的多重輸出裝置

> 這一步的作用：系統聲音（會議對方講的話）會同時送到你的耳機（你聽得到）和 BlackHole（可以被錄下來）。

## 4. 建立「聚集裝置」（把你的麥克風 + 對方聲音合併成一路輸入）

1. 同樣在 **音訊 MIDI 設定**，左下角點 `+` → **建立聚集裝置**
2. 勾選：你的麥克風（例如 MacBook Pro Microphone）＋ **BlackHole 2ch**
3. 這個聚集裝置就是等一下要餵給 `record.sh` 的輸入來源

> 這一步的作用：你的聲音（麥克風）和對方的聲音（透過 BlackHole 迴路）會被合併成同一個輸入裝置，錄下來就是完整的雙向對話。

## 5. 設定 record.sh 要用哪個裝置

```
./scripts/record.sh setup
```

會列出所有 avfoundation 音訊裝置，選擇上一步建立的**聚集裝置**編號即可，設定只需要做一次（除非你换了電腦或重建裝置）。

## 常見問題

- **開會時聽不到自己的聲音 / 有回音**：檢查系統輸出是否真的選到「多重輸出裝置」，並確認會議軟體（Zoom / Meet / Teams）的麥克風輸入是選一般麥克風，而不是聚集裝置（避免自己講話被送進喇叭造成回授）。
- **錄出來沒有對方的聲音**：確認系統輸出（系統設定 → 聲音 → 輸出）真的切到多重輸出裝置，而不是變回耳機/喇叭本身。
- **sample rate 不一致造成雜音**：多重輸出裝置與聚集裝置裡的每個子裝置，建議都手動對齊成同一個取樣率（點裝置右側齒輪 → 設定取樣率）。
