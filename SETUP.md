# 環境設定：BlackHole + 多重輸出 / 聚集裝置

這份教學只需要做一次。做完之後，錄音就直接用 macOS 內建的 `Cmd+Shift+5` 或 QuickTime Player，細節看 [USAGE.md](./USAGE.md)。

## 1. 安裝 BlackHole

```
brew install blackhole-2ch
```

裝完後**登出再登入**（或重開機），macOS 才會正確載入這個虛擬音效卡。

## 2. 建立「多重輸出裝置」（讓你自己還聽得到聲音）

1. 打開 **音訊 MIDI 設定**（Spotlight 搜尋 "Audio MIDI Setup"）
2. 左下角點 `+` → **建立多重輸出裝置**
3. 勾選：你平常的輸出（例如耳機 / 喇叭）＋ **BlackHole 2ch**
4. 把你平常的輸出設為「主要裝置」（Primary Device），並確保它跟 BlackHole 對齊同一個 sample rate（例如都設 44100 Hz 或都設 48000 Hz）
5. 在下方清單裡，把**非主要裝置**（也就是 BlackHole 2ch）的 **Drift Correction** 打勾

> 這一步的作用：系統聲音（會議對方講的話）會同時送到你的耳機（你聽得到）和 BlackHole（可以被錄下來）。

## 3. 建立「聚集裝置」（把你的麥克風 + 對方聲音合併成一路輸入）

1. 同樣在 **音訊 MIDI 設定**，左下角點 `+` → **建立聚集裝置**
2. 勾選：你的麥克風（例如 MacBook Pro Microphone，或有線耳麥的話是 External Microphone）＋ **BlackHole 2ch**
3. Clock Source 選 **BlackHole 2ch**，把**麥克風**的 **Drift Correction** 打勾
4. Sample Rate 設成跟多重輸出裝置一致

> 這一步的作用：你的聲音（麥克風）和對方的聲音（透過 BlackHole 迴路）會被合併成同一個輸入裝置，錄下來就是完整的雙向對話。

## 常見問題

- **開會時聽不到自己的聲音 / 有回音**：檢查系統輸出是否真的選到「多重輸出裝置」，並確認會議軟體（Zoom / Meet / Teams）的麥克風輸入是選一般麥克風，而不是聚集裝置（避免自己講話被送進喇叭造成回授）。
- **錄出來沒有對方的聲音**：確認系統輸出（系統設定 → 聲音 → 輸出，或按住 `Option` 點選單列喇叭圖示）真的切到多重輸出裝置，而不是變回耳機/喇叭本身。
- **sample rate 不一致造成雜音**：多重輸出裝置與聚集裝置裡的每個子裝置，務必手動對齊成同一個取樣率（點裝置右側齒輪 → 設定取樣率）。
- **換了耳機（有線 → 藍牙，或反過來）之後聲音不對**：多重輸出裝置和聚集裝置裡勾的是**具體的裝置名稱**，換一種耳機接法可能會讓「你現在實際在用的麥克風/喇叭」對應到不同的系統裝置名稱。回頭到音訊 MIDI 設定檢查多重輸出裝置、聚集裝置是否還勾著正確的實體裝置，需要的話重新勾選並存檔即可，不用重建整個裝置。
