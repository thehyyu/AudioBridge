# 操作說明

這份是「設定都弄好之後，每次要錄音時該做什麼」的說明。一次性的裝置設定教學在 [SETUP.md](./SETUP.md)。

錄製本身用 macOS 內建工具：**螢幕錄影**用 `Cmd+Shift+5`，**純音訊**用 QuickTime Player 的「新增音訊錄製」。兩者都要把麥克風選成你建立的 **Aggregate Device（聚集裝置）**，才會同時收到你的聲音和對方的系統聲音。

## 錄製前：把系統輸出切到多重輸出裝置

這樣你自己才聽得到聲音，同時聲音也會被送進 BlackHole 供錄音使用。

1. 按住鍵盤上的 **`Option`** 鍵不放
2. 用滑鼠點螢幕右上角選單列的**喇叭圖示** 🔊
3. 選單裡選 **Multi-Output Device**（多重輸出裝置）

> 如果按 Option 點喇叭圖示沒有跳出裝置清單：改到 **系統設定 → 聲音 → 輸出**，手動點選「Multi-Output Device」。

## 螢幕錄影（畫面 + 系統音 + 麥克風）

1. 按 **`Cmd + Shift + 5`**，畫面下方會跳出一條工具列
2. 選 **「錄製整個畫面」**（方框圖示）或 **「錄製選取範圍」**（虛線框圖示）
3. 點工具列上的 **「選項」（Options）**：
   - **麥克風**：選你的 **Aggregate Device**（聚集裝置）—— 這個選單預設常常停在「None」，很容易漏改就直接開始錄，錄完整段完全靜音，**務必點開確認真的選到 Aggregate Device**
   - **儲存位置**：可以順手改成 `recordings/` 資料夾（選單裡「其他位置...」可以自己選）
4. 選「錄製整個畫面」的話直接點畫面任一處開始倒數；選「錄製選取範圍」的話先拖出範圍，再點 **「錄製」**
5. 結束時點選單列上方的**停止方塊圖示** ⏹️（或再按一次 `Cmd+Shift+5`，工具列上有停止按鈕）

停止後幾秒會自動彈出縮圖，點一下可以打開影片確認。

## 純音訊錄製（不含畫面）

1. 打開 **QuickTime Player**
2. 選單列 **檔案 → 新增音訊錄製**
3. 點錄製按鈕旁邊的小箭頭 ⌄，選擇輸入裝置為 **Aggregate Device**
4. 點錄製開始，講話/播放讓兩邊聲音都進來
5. 結束按停止，**檔案 → 儲存**，選好路徑（例如 `recordings/`）存檔

## 錄完之後：把系統輸出切回來

跟錄製前的步驟一樣，按住 `Option` 點喇叭圖示，切回你原本平常用的輸出（例如耳機）。**這步不會自動做，容易忘記**，忘記的話之後聽音樂/看影片聲音會怪怪的（因為還送到多重輸出裝置），發現不對勁回來切一下就好，不影響已經錄好的檔案。

## 如果錄出來沒聲音

- 確認錄製選項裡的**麥克風**真的選到 Aggregate Device，不是 None 或內建麥克風（最常見的原因）
- 確認錄製前有把系統輸出切到 Multi-Output Device，不然對方的聲音不會進到 BlackHole
- 第一次用這個功能可能會跳出「是否允許存取麥克風」的權限詢問，記得允許；也可以到 **系統設定 → 隱私權與安全性 → 麥克風** 確認有給對應的 App 授權
- 確認 Audio MIDI 設定裡，聚集裝置的兩個子裝置（BlackHole 2ch、你實際在用的麥克風）都還在勾選狀態（重開機、拔插裝置後有時候會被系統重置）
